/**
 * EventBus — PLACEHOLDER intencional.
 *
 * Princípio 5 (não-negociável): "Chamadas diretas síncronas entre services.
 * EventBus apenas se aparecer ciclo real, NUNCA preventivamente."
 *
 * Este arquivo existe APENAS pra reservar o nome. NÃO IMPLEMENTAR a menos que:
 * 1. Ciclo real seja detectado (madge --circular reportar)
 * 2. Refactor service-to-service direta seja insuficiente
 * 3. Decisão arquitetural explícita seja registrada em ADR
 *
 * Não importar deste arquivo. Não usar.
 */

export const EVENT_BUS_NOT_IMPLEMENTED = true;
